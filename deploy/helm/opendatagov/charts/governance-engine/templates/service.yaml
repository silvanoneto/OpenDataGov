{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "governance-engine.fullname" . }}
  labels:
    {{- include "governance-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "governance-engine.selectorLabels" . | nindent 4 }}
{{- end }}
