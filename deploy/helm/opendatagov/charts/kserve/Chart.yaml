apiVersion: v2
name: kserve
version: 0.1.0
appVersion: "0.12.0"
description: KServe model serving with B-Swarm governance and observability
keywords:
  - kserve
  - model-serving
  - mlops
  - inference
home: https://github.com/silvanoneto/opendatagov
maintainers:
  - name: OpenDataGov Team

dependencies:
  # KServe requires cert-manager for webhook certificates
  - name: cert-manager
    version: "v1.14.x"
    repository: https://charts.jetstack.io
    condition: certManager.enabled
